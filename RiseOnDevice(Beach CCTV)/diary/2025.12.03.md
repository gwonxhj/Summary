# 2025.09.17 ~ ing
- YOLO 관련 논문 수집 후 주 1회 세미나 리뷰
- Raspberry Pi zero 2 W를 이용하여 OS 설치해보기(raspberry pi os, ubuntu)
- 업체로부터 제공받은 데이터셋(image, label, test) 활용하여 pt파일 학습
- 학습받은 pt파일 -> onnx -> rknn 변환(Odroid M1 보드에서 사용 위함)
- 학습받은 pt파일 -> onnx -> engine 변환(Jetson Orin Nano 보드에서 사용 위함)
- 프로젝트에 사용하기 위한 보드 선정(Raspberry Pi, Odroid m1/m2, Jetson Orin Nano, Jetson AGX Orin)

# onnx -> rknn 변환 단계에서 "RKNN module" 호출 안 되는 문제 해결
- onnx -> rknn으로 python을 이용한 변환 시도했으나 "rknn module not found error"로 변환 안 되는 상황 발생(rknn toolkit lite 1.7.5버전으로 시도)
- 기존 툴킷 제거 후 rknn toolkit2 1.6.0으로 설치하고 변환하니 성공
- rknn toolkit lite = "변환" 기능은 없고 "추론" 기능만 포함

# rknn 파일로 Odroid M1(Ubuntu 20.04)에서 C++로 추론
- main.cpp 파일 생성 및 make를 통해 yolo_run파일 생성
- FPS, F1 score, mAP@50, precision 벤치마크 결과값 추출하기 위한 추론
- YOLOv8s.rknn 모델 기준 FPS = 9.10 값만 나오고 나머지 항목들 0으로 나옴 ****** 문제
  -> 기존에 학습시킨 pt파일에 문제가 있는 것 같음 재학습 후 추론해볼것
