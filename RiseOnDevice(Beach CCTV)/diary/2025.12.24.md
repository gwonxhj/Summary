# 문제 발생
- yolov8n(int8).rknn 파일 추론 시 추론 결과 True Positive: 0, False Positive: 179,941 출력
  -> 커널 버전 0.4.2(구형 드라이버)문제로 판단하고 커널만 업데이트 시도
- Odroid M1, Ubuntu 20.04 LTS 커널 버전 4.19.219(기존 사용)에서 하드커널 공식 저장소를 이용해서 커널만 업데이트 시도하다가 무한 로딩 현상 발생

# 문제 해결 과정
- 공식 위키에 있는 이미지 말고 TheRemote/Legendary-ODROID-M1 깃허브 페이지에서 22.04.1 새로 구워서 적용
- RKNN toolkit2 2.3.2, 커널 버전 6.0.0으로 세팅
- images, labels, CMakeLists.txt, rknn 파일 등 세팅은 다 해둠 추론부터 진행해볼것

# NEW
- sed -i 's/\xc2\xa0/ /g' <filename>
  -> 특수 공백 제거 명령어
